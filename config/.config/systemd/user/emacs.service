[Unit]
Description=Emacs: the extensible, self-documenting text editor

[Service]
Type=forking
ExecStart=/usr/bin/emacs --daemon
ExecStop=/usr/bin/emacscleint --eval "(kill-emacs)"
Restart=always

[Install]
WantedBy=default.target
